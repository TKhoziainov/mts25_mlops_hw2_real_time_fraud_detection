## 🏗️ Архитектура

Компоненты системы:
1. **`interface`** (Streamlit UI):
   
   Создан для удобной симуляции потоковых данных с транзакциями. Реальный продукт использовал бы прямой поток данных из других систем.
   
   **Вкладка "Отправка данных":**
    - Имитирует отправку транзакций в Kafka через CSV-файлы.
    - Генерирует уникальные UUID для каждой транзакции.
    - Загружает транзакции отдельными сообщениями формата JSON в топик Kafka `transactions`.
    - Отображает прогресс отправки данных.
    - Поддерживает загрузку нескольких файлов с отслеживанием статуса каждого.
   
   **Вкладка "Результаты скоринга":**
    - Отображает результаты применения модели к транзакциям из базы данных PostgreSQL.
    - Показывает метрики: общее количество транзакций, количество мошеннических транзакций, процент мошенничества.
    - Выводит детальную таблицу всех результатов скоринга с полями: `transaction_id`, `score`, `fraud_flag`, `created_at`.
    - Отображает таблицу только мошеннических транзакций (топ-10 последних).
    - Строит гистограмму распределения скоринговых оценок с равномерными интервалами (20 бинов по 0.05).
    - По умолчанию загружает последние 100 записей (настраивается через переменную окружения `RESULTS_LIMIT`).
    

2. **`fraud_detector`** (ML Service):
   - Загружает предобученную модель CatBoost (`my_catboost.cbm`).
   - Выполняет препроцессинг данных:
     - Извлечение временных признаков
     - Гео-расстояния
     - Кодирование категориальных переменных
   - Производит скоринг с порогом 0.98.
   - Выгружает результат скоринга в топик Kafka `scores`

3. **Kafka Infrastructure**:
   - Zookeeper + Kafka брокер
   - `kafka-setup`: автоматически создает топики `transactions` и `scores`
   - Kafka UI: веб-интерфейс для мониторинга сообщений (порт 8080)

4. **PostgreSQL Database Services**:
   
   **`postgres`** (База данных):
    - PostgreSQL 15 (Alpine образ для минимального размера).
    - Хранит результаты скоринга в таблице `fraud_scores`.
    - Схема таблицы:
      - `id`: первичный ключ (SERIAL)
      - `transaction_id`: уникальный идентификатор транзакции (VARCHAR, UNIQUE)
      - `score`: скоринговая оценка (DECIMAL 10,6)
      - `fraud_flag`: флаг мошенничества (INTEGER: 0 или 1)
      - `created_at`: временная метка создания записи (TIMESTAMP)
    - Индексы для оптимизации запросов по `transaction_id`, `fraud_flag` и `created_at`.
    - Автоматическая инициализация схемы через `init_db.sql` при первом запуске.
    - Данные сохраняются в Docker volume `postgres_data` для персистентности.
    - Порт 5432 доступен на хосте для прямого подключения.
   
   **`score_writer`** (Сервис записи):
    - Читает сообщения из топика Kafka `scores`.
    - Записывает результаты скоринга в базу данных PostgreSQL.
    - Обрабатывает сообщения в формате JSON с полями: `transaction_id`, `score`, `fraud_flag`.
    - Использует `ON CONFLICT` для обновления существующих записей (upsert).
    - Автоматически переподключается к базе данных при сбоях (до 5 попыток).
    - Логирует все операции для мониторинга и отладки.

## 🚀 Быстрый старт

### Требования
- Docker 20.10+
- Docker Compose 2.0+

### Настройка

Перед запуском создайте файл `.env` в корне проекта с настройками базы данных:

```bash
cp env.example .env
```

Отредактируйте `.env` и установите безопасные пароли:

```env
POSTGRES_DB=fraud_detection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
```

### Запуск
```bash
# Сборка и запуск всех сервисов
docker compose up --build
```
После запуска:
- **Streamlit UI**: http://localhost:8501
- **Kafka UI**: http://localhost:8080
- **Логи сервисов**: 
  ```bash
  docker compose logs <service_name>  # Например: fraud_detector, kafka, interface
  ```

## 🛠️ Использование

### 1. Загрузка данных:

 - Загрузите CSV через интерфейс Streamlit. Для тестирования работы проекта используется файл формата `test.csv` из соревнования https://www.kaggle.com/competitions/teta-ml-1-2025
 - Пример структуры данных:
    ```csv
    transaction_time,amount,lat,lon,merchant_lat,merchant_lon,gender,...
    2023-01-01 12:30:00,150.50,40.7128,-74.0060,40.7580,-73.9855,M,...
    ```
 - Для первых тестов рекомендуется загружать небольшой семпл данных (до 100 транзакций) за раз, чтобы исполнение кода не заняло много времени.

### 2. Мониторинг:
 - **Kafka UI**: Просматривайте сообщения в топиках transactions и scores
 - **Логи обработки**: /app/logs/service.log внутри контейнера fraud_detector

### 3. Результаты:

 - Скоринговые оценки пишутся в топик scores в формате:
    ```json
    {
    "score": 0.995, 
    "fraud_flag": 1, 
    "transaction_id": "d6b0f7a0-8e1a-4a3c-9b2d-5c8f9d1e2f3a"
    }
    ```
## Структура проекта
```
.
├── fraud_detector/
│   ├── app/
│   │   └── app.py              # Kafka Consumer/Producer
│   ├── src/
│   │   ├── preprocessing.py   # Логика препроцессинга
│   │   └── scorer.py           # ML-модель и предсказания
│   ├── models/
│   │   └── my_catboost.cbm     # Предобученная модель
│   ├── train_data/
│   │   └── train.csv           # Тренировочные данные
│   ├── requirements.txt
│   └── Dockerfile
├── interface/
│   ├── app.py                  # Streamlit UI
│   ├── requirements.txt
│   └── Dockerfile
├── score_writer/
│   ├── app.py                  # Сервис записи скорингов из Kafka в Postgres
│   ├── Dockerfile
│   └── requirements.txt
├── docker-compose.yaml
├── .env.example                # Пример файла переменных окружения (Переименуйте в .env, заполните своими значениями)
├── init_db.sql                 # Скрипт инициализации таблиц в базе данных Postgres
└── README.md
```

## Настройки Kafka
```yml
Топики:
- transactions (входные данные)
- scores (результаты скоринга)

Репликация: 1 (для разработки)
Партиции: 3
```

*Примечание:* 

Для полной функциональности убедитесь, что:
1. Модель `my_catboost.cbm` размещена в `fraud_detector/models/`
2. Тренировочные данные находятся в `fraud_detector/train_data/`
3. Порты 2181 (Zookeeper), 5432 (PostgreSQL), 8080 (Kafka UI), 8501 (Streamlit) и 9095 (Kafka) свободны на хосте
